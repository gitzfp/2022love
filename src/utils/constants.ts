export const BrightenModeKey = "isBrighten";

export enum MoneyType {
    USDT = 1,
    ETH = 2,
    BTC = 3,
    OWL = 4,
}

export enum LanguageType {
    EN = "en",
    JA = "ja",
}

export enum LoginType {
    HIDE = "HIDE",
    LOGIN = "login",
    REGIST = "regist"
}
